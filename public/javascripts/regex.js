// some regex training

const srtingOne   = 'This is the longest string ever.',
      srtingTwo   = 'this is the shotest string ever.',
      srtingThree = 'Is this is the thing called Mount Everest?',
      srtingFour = 'This is the Sherman on Mount.';


  // const regex = /this/;

  // console.info(regex.test(srtingOne));
  // console.info(regex.test(srtingTwo));
  // console.info(regex.test(srtingThree));
  // console.info(regex.test(srtingFour));

  // const regex = /this/i;

  // console.info(regex.test(srtingOne));
  // console.info(regex.test(srtingTwo));
  // console.info(regex.test(srtingThree));
  // console.info(regex.test(srtingFour));

  // const regex = /^this/i;

  // console.info(regex.test(srtingOne));
  // console.info(regex.test(srtingTwo));
  // console.info(regex.test(srtingThree));
  // console.info(regex.test(srtingFour));

// const regex = /this$/i;

//   console.info(regex.test(srtingOne));
//   console.info(regex.test(srtingTwo));
//   console.info(regex.test(srtingThree));
//   console.info(regex.test(srtingFour));

const regex = /this$/i;

  console.info(regex.test(srtingOne));
  console.info(regex.test(srtingTwo));
  console.info(regex.test(srtingThree));
  console.info(regex.test(srtingFour));

